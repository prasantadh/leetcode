#include "tags.h"
#include <cassert>
#include <iostream>
#include <unordered_map>
#include <vector>

using namespace std;

vector<Tags> tags = {Tags::FIXME};

class Solution {
public:
  bool solve() { return true; }
};

int main(int argc, char **argv) {
  Solution solution = Solution();
  // vector input1<int> = {...};
  // assert(solution.solve(input1) == true);
  return 0;
}
